import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    paddingBottom: 20,
    flex: 1,
  },
  scrollContainer: {
    // alignItems: 'center',
  },
  googleButton: {
    width: 200,
    height: 48,
  },
});
